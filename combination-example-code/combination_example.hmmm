-- Assembly Language Combination Calculator
-- Calculates C(n, k) = n! / (k! * (n-k)!)
-- 
-- Register conventions:
--   r13 = return value
--   r14 = return address
--   r15 = stack pointer
--   r0-r12 = general purpose

-- Main program
    READ r1             -- Read n into r1
    READ r2             -- Read k into r2
    
    HALT